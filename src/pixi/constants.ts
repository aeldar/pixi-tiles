export const TILE_SIZE_PX = 1024;
export const INITIAL_WORLD_DOCUMENT_WIDTH = 200; // px
export const LOD_MAX_LEVEL = 5; // max level of detail, zero based
export const DEBUG_TILES = true; // if true, the tile will have a border